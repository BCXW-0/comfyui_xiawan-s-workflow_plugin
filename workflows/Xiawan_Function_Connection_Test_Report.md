# Xiawan Function and Connection Test Report

Test date: 2026-08-01

## Scope

- Workflow: `Xiawan's Workflow.json`
- ComfyUI: `0.8.2`
- GPU: NVIDIA GeForce RTX 4060 Laptop GPU (8 GiB VRAM)
- Test method: live ComfyUI API after a launcher-managed restart. No ComfyUI process was started from the command line.

## Repairs Made

1. Moved Xiawan's 29 required third-party node types into the private `vendor` runtime in this plugin, and registered only the types used by the workflow.
2. Replaced the vendor gallery package initializer with a minimal private namespace. This prevents unrelated gallery metadata hooks and Web API registration from running inside Xiawan.
3. Repaired the two `TriggerWord Toggle (LoraManager)` nodes. Their historical serialized value for `allow_strength_adjustment` was an empty list, which is invalid for the current Boolean input. Both are now explicitly `false`.
4. Replaced the remaining legacy `CodexGlobalSeedManager` metadata lookup with `XiawanGlobalSeedManager`. No registered node type contains `codex`.
5. Kept all workflow geometry unchanged during functional repair. All nodes and groups retain their existing position/size locks.

## Dependency Isolation

- Required external workflow types: 29
- Isolated Python import: 29/29 registered successfully from Xiawan's private vendor tree.
- Live ComfyUI after restart: 29/29 registered successfully.
- Live `python_module` source: all 29 report `custom_nodes.comfyui_xiawan's-workflow_plugin`.
- Non-Xiawan custom-node packages were moved out of `ComfyUI/custom_nodes` before the live test. ComfyUI then ran Xiawan without those packages being registered.

## Workflow and Connection Audit

- Nodes: 224
- Groups: 35
- Links: 563
- Registered node types used by the workflow: 84/84
- Missing node types: 0
- Link validation errors after type normalization: 0
- Position, size, and layout locks: nodes 224/224; groups 35/35
- Node registration names containing `codex`: 0

## Live Execution Results

### Preflight

- API prompt conversion: 224/224 nodes, 0 missing required inputs.
- The first API queue attempt was rejected before execution because the test converter did not initially account for two front-end-only serialization patterns (LoRA trigger widgets and FaceDetailer seed mode). No image was generated by that rejected attempt.
- After the compatibility repair, the API accepted the complete prompt with `node_errors: {}`.

### Final cold-start test

- Prompt ID: `c2c761ba-46e9-40dd-af20-7d622bca31c2`
- Status: `execution_success`
- Cached nodes after restart: none
- Runtime: about 148 seconds
- Saved final image: `output/2026-08-01/09-00-48-14954619253030047831_xiawan_00001_.png`
- Final image size: `1040 x 1520`
- Base comparison image: `832 x 1216`
- Final comparison image: `1040 x 1520`

The configured latent-upscale factor is `1.25`; therefore `832 x 1.25 = 1040` and `1216 x 1.25 = 1520`. The produced final resolution matches the active configuration.

## Visual Check

The final PNG was opened and inspected after execution. It is non-empty, has the expected portrait framing, and contains a complete rendered image without a blank canvas or output corruption.

## Coverage Note

This report verifies the full executable graph under the workflow's current configuration, including final save and base/final comparison outputs. Mutually exclusive or disabled optional branches were registered and connection-validated, but were not all force-enabled simultaneously because that would no longer be the user's current configuration and can substantially increase VRAM demand.

## UI Migration Repair Follow-up

Follow-up date: 2026-08-01

### Repaired components

- Restored the native LoRA Manager front-end implementation for `Lora Loader (LoraManager)` and `TriggerWord Toggle (LoraManager)`, including their internal widget modules.
- Corrected vendored ComfyUI imports to `/scripts/app.js` and `/scripts/api.js` where the modules are served below nested extension paths.
- Restored the native PromptSelector front end and its server-backed prompt library controls.
- Restored `WeiLinPromptUIWithoutLora` with the isolated WeiLin editor runtime, editor button, random-template behavior, static files, and API routes.
- Disabled remote browser log submission by default so the migrated PromptSelector front end no longer calls an absent logging endpoint.

### LoRA Manager version integrity

The incomplete vendor tree contained all 153 Python files but omitted the runtime assets required by `LoraManager.add_routes()`. The complete upstream snapshot was identified by normalized file comparison as commit `78303b2a5e5b247fea6e1d0757683d06c383ac6b`.

- Python tree match: `153/153`
- Added static assets: `179` files, `11,463,617` bytes
- Added templates: `32` files, `235,618` bytes
- Added locales: `10` files, `1,109,229` bytes
- Added `settings.json.example`: `595` bytes
- Post-copy SHA-256 mirror mismatches: `0`

### Isolated cold-start route probes

The probes used in-memory `aiohttp` applications and did not bind a port, start ComfyUI, stop ComfyUI, or operate the launcher.

- LoRA Manager routes registered: `265`
- `/api/lm/version-info`: present
- `/api/lm/loras/list`: present
- `/api/lm/loras/get_trigger_words`: present
- `/loras_static`: present
- `/locales`: present
- WeiLin editor routes registered: `95`
- Existing WeiLin databases passed their integrity checks. The optional online Danbooru refresh was skipped after GitHub returned an anonymous rate-limit response.

### Three-pass locked-layout audit

All three independent reads produced the same result:

- Workflow SHA-256: `f75c563992236956ce5184f85f15845919888a982e2762ac6860b3d12fdacb5d`
- Nodes / links / groups: `224 / 563 / 35`
- Dangling links: `0`
- Node overlaps: `0`
- Partial non-parent group overlaps: `0`
- Locked nodes / groups: `224 / 35`
- Round-shape nodes: `224`
- Target-node geometry mismatches: `0`
- JavaScript syntax checks: `42/42` passed in each of three rounds
- Registration/source scan containing `codex`: `0`

Current target node geometry:

| Node IDs | Type | Size |
| --- | --- | --- |
| `10`, `261` | `Lora Loader (LoraManager)` | `400 x 420` |
| `11`, `260` | `TriggerWord Toggle (LoraManager)` | `360 x 360` |
| `129`, `130` | `PromptSelector` | `480 x 480` |
| `126`, `127` | `WeiLinPromptUIWithoutLora` | `480 x 300` |

### Prompt editor layout adjustment

Adjustment date: 2026-08-02

- Doubled the height of `D站前缀` and `D站后缀` from `240` to `480` without changing their width.
- Reflowed the four prompt nodes into a two-column grid: `10` px horizontal gap and `40` px vertical gap.
- Moved group `24` and its four nodes to the left of `0-a~0-c3 姿态 / 人物 / 结构控制`.
- New group `24` bounds: `[-3750, -545, 990, 1144]`.
- Gap to the control group: `10` px; both groups are top-aligned and have the same `1144` px height.
- Minimum group padding is satisfied: `10` px left/right, `70` px top, and at least `10` px bottom.
- Three fresh disk reads passed with `0` margin violations, `0` spacing violations, `0` target overlaps, and `0` global node overlaps.
- Geometry changes outside nodes `126`, `127`, `129`, `130` and group `24`: `0`.
- Links changed: `0`.
- The two required node types are registered in the running ComfyUI `/object_info`: `PromptSelector` and `WeiLinPromptUIWithoutLora`.
- The already-open main workflow tab retained its pre-adjustment in-memory canvas. That stale tab was not used as visual evidence and must be closed and reopened before judging the new disk layout.

### Current live-process status

The currently running launcher-managed ComfyUI process was started before the LoRA runtime assets were restored. It therefore still reports:

- `/prompt_selector/data`: HTTP `200`
- `/prompt_selector/metadata`: HTTP `200`
- `/api/lm/version-info`: HTTP `404`
- `/api/lm/loras/list`: HTTP `404`

This is an expected stale-process result, not a cold-start registration failure. A launcher-managed restart is required before the LoRA API and final browser interaction can be rechecked. No restart was performed by the repair process.

Chrome UI automation could not be repeated after this file-level repair because the ChatGPT Chrome extension is not installed in the active Chrome profile. Earlier live canvas inspection in the same repair session confirmed that all eight target nodes had recovered real widgets without changing their serialized geometry. Final post-restart interaction testing remains pending and must not be reported as passed until the restarted server returns the LoRA API successfully and the editor/LoRA controls are exercised in the browser.
