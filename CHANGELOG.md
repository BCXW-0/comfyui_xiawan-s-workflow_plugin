# Changelog

## R-1.4.1 - 2026-08-06

### 修复

- 修复 LoRA Manager 扫描缓存尚未完成或未包含新文件时，把无扩展名 LoRA 名称直接传给文件系统导致加载失败的问题。
- LoRA 现在统一通过 ComfyUI `folder_paths` 解析，支持带扩展名、无扩展名、子目录和直接文件路径；无法解析的条目会被跳过并记录警告，不再让整个队列因 `FileNotFoundError` 中断。
- 同步修复标准 LoRA、Nunchaku Flux 和 Nunchaku Qwen-Image 分支的路径解析。

### ComfyUI 兼容性

- 按 ComfyUI 0.32.2 兼容性要求复核 `folder_paths.get_full_path`、`comfy.utils.load_torch_file` 和 `comfy.sd.load_lora_for_models` 的调用方式，保留 0.28.0 及以上运行时的稳定接口。
- R-1.4.0 的发布工作流和开发者实际运行工作流均不修改；R-1.4.1 仅包含 LoRA 加载与兼容性修复。

## R-1.4.0 - 2026-08-06

### ComfyUI 兼容性

- 适配 ComfyUI `0.28.0` 与前端 `1.45.21`，Anima 运行时明确要求原生 `Anima`、`LLMAdapter` 和 Qwen3 文本编码能力。
- 保留旧运行时的提前失败保护，避免模型被错误识别后继续生成伪成功的全幅彩色噪声图。

### Anima 功能链路

- 新增分支 VAE 切换：图生图编码在 SDXL 与 Anima 分支分别使用对应 VAE。
- 新增 Anima 图生图 latent 切换：自采样使用 Anima 空 latent，图生图直通/重采样使用输入图的 Anima VAE 编码结果。
- 将 Anima 清洁模板默认参数调整为 30 steps、CFG 4、`er_sde`、`simple`，并保留 1024 级以上分辨率的可调输入。

### 工作流与发布边界

- 发布 `Xiawan's Workflow ver.R-1.4.0.json`，保留 R-1.3.0 的 224 个旧节点位置和尺寸，新增 2 个隐藏逻辑节点。
- 新模板共 226 个节点、569 条连线、35 个分组；新增节点放置于现有空白区域，矩形审计无重叠。
- 发布模板继续清除 Checkpoint、LoRA、提示词和个人图片，输入图统一使用 `example.png` 占位图。
- `Xiawan's Workflow.json` 仍仅用于开发者实际运行配置，不作为发布模板使用；WeiLin 数据库、模型、LoRA 和输出图不进入发布包。

## R-1.3.0 - 2026-08-06

### 修复

- 修复 Anima 底图分支在旧版运行时被误判为其他扩散模型、导致输出全幅彩色噪声的问题。
- 使用 ComfyUI 原生 Anima / `LLMAdapter` / Qwen3 文本编码链路；不再调用旧的兼容性文本编码器。
- 保存元数据节点现在能从当前工作流、工作流字符串和组状态缓存读取 Anima 参数，并记录 Anima 模型、步数、CFG、采样器和调度器。
- 缺少原生 Anima 支持时提前报错并提示升级，避免生成伪成功结果。

### 运行时

- 开发验证基线为 ComfyUI 提交 `2500d0fcfb973724731824e829276e073bd9b396`（`0.15.1`）或更新版本。

### 工作流

- 发布 `Xiawan's Workflow ver.R-1.3.0.json`。
- 保持 224 个节点、563 条连线、35 个组，以及现有节点位置、尺寸和组边界不变。
- 发布模板继续清除 Checkpoint、LoRA、提示词和个人图片，输入图统一使用 `example.png` 占位图。
- `Xiawan's Workflow.json` 仍仅用于开发者实际运行配置，不作为发布模板。

## R-1.2.4 - 2026-08-05

### 修复

- 智能 Tag 文档导入现在按一级标题、二级标题、Tag 内容和 Tag 描述四项完全一致判断重复项，重复记录不会再次导入。
- 同一分类下只合并新增 Tag；相同 Tag 内容但描述不同的记录会保留为新增项。
- 导入完成提示增加新增数量和跳过重复数量。

### 工作流

- 发布 `Xiawan's Workflow ver.R-1.2.4.json`，保持发布模板的模型、LoRA、提示词为空，图片输入使用占位图。

## R-1.2.3 - 2026-08-05

### 修复

- 修复 WeiLin 标签数据库较大时，启动初始化和全量 Tag JOIN 占用过多时间，导致 ComfyUI WebUI 长时间无响应的问题。
- 标签管理默认只读取分类和分组，并按页读取 Tag；首批 200 条，单次请求最多 500 条，继续使用“加载更多标签”即可访问后续内容。
- 保留全量 Tag 接口供导出功能使用，避免影响现有标签导出操作。

### 数据与工作流

- 数据库只保留在开发者运行目录，桌面工作区和 GitHub 发布内容仅同步读取逻辑、前端代码、文档和清洁工作流模板。
- 发布 `Xiawan's Workflow ver.R-1.2.3.json`，保持发布模板的模型、LoRA、提示词为空，图片输入使用占位图。

## R-1.2.2 - 2026-08-04

### 修复

- 修复“最终输出 · 底图/结果对比”节点生成图片后仍显示空白的问题。
- 对比节点现在同时提供标准预览兼容数据和双图对比滑块，批量结果可逐项切换。
- 底图生成完成后会先在对比预览中显示图像 A，最终图像到达后再显示完整对比。

### 工作流

- 发布 `Xiawan's Workflow ver.R-1.2.2.json`，保留现有节点、连线、位置和尺寸。

## R-1.2.1 - 2026-08-04

### 修复

- 智能 Tag 文档导入改用插件内置的旧式 OLE/Word 兼容文档读取器，`.doc` 不再要求安装 LibreOffice。
- 导入完成后先刷新标签管理面板，再显示“导入成功，标签管理面板已自动刷新，请返回标签管理面板查看。”提示，减少用户误判。
- OLE 结构异常、加密文档和无法解析的 Word 文本会返回可读的导入错误。

### 兼容性

- 旧式 DOC 及常见 Word/WPS 兼容格式可直接读取正文和段落文本；当前不承诺保留原文档的全部排版特性。
- DOCX 继续保留标题层级识别，TXT、DOC、DOCX 均不需要额外办公软件转换。

### 工作流

- 发布 `Xiawan's Workflow ver.R-1.2.1.json`，节点、连线、分组、位置和尺寸与 R-1.2.0 保持一致。

## R-1.2.0 - 2026-08-04

### 新增

- 为 WeiLin 标签管理增加“智能法典分析”，支持导入 TXT、DOC 和 DOCX 文档。
- 自动读取文档分类层级、Tag 和说明，生成可执行的 Tags SQL，并在导入前进行语法校验。
- 导入成功后自动刷新标签管理界面，并显示一级分类、二级分类和 Tag 数量。
- 导入 SQL 会保存在 WeiLin 用户数据目录的 `intelligent_tag_imports` 文件夹中，便于回溯。

### 兼容性

- 为导入记录补齐旧版管理接口使用的分类关联字段。
- 分类、Tag 文本和同类出现序号生成稳定 UUID，重复导入或修改描述时不会因描述变化产生新的 Tag 身份。
- DOC 格式需要系统安装 LibreOffice；TXT、DOCX 无额外转换依赖。

### 工作流

- 发布 `Xiawan's Workflow ver.R-1.2.0.json`，沿用 R-1.1.1 的节点、连线、分组、位置和尺寸。

## R-1.1.1 - 2026-08-04

### 修复

- 修复正面提示词和负面提示词节点底部的“打开提示词编辑器”按钮无法打开 WeiLin 全局提示词 UI 的问题。
- 修复 WeiLin 提示词编辑器资源并发加载时的等待状态，并为隐藏 JSON 参数增加容错，避免按钮因异常内容失去响应。

### 工作流

- 发布 `Xiawan's Workflow ver.R-1.1.1.json`，节点、连线、分组、位置和尺寸与 R-1.1 保持一致。

## R-1.1 - 2026-08-04

### 修复

- 将发布模板明确为 `Xiawan's Workflow ver.R-1.1.json`，并区分发布模板与开发者本地运行配置。
- 修复组状态读取：优先使用当前工作流元数据，未知组不再默认开启。
- 移除全局 Seed 的跨任务缓存，`last/increase/decrease` 优先使用工作流中的当前 Seed。
- 将 Seed 矩阵的首 Seed 接入采样、放大、细化和元数据路径；关闭矩阵应用时保持全局 Seed 行为。
- 保存元数据时读取当前工作流的模型、LoRA、采样参数和分辨率，减少硬编码信息。
- 统一限制 Tiled VAE 的 `overlap`，避免超过 `tile_size / 4`。
- 显存整理节点改为在低于阈值时卸载模型；未知显存状态仍保持保守释放。
- 第三方节点按模块隔离注册，单个可选依赖失败时不再阻断其他模块的注册。
- 修复直接暴露类的第三方模块注册，恢复 Mira、Impact Pack 和 LoRA Manager 工作流节点。

### 工作流

- 保留 R-1.0 的节点、连线、分组、位置和尺寸。
- R-1.1 发布模板清除 Checkpoint、LoRA、提示词和个人输入图，输入图统一使用 `example.png` 占位图。
- 修正工作流结构元数据，节点数为 224，连线数为 563，分组数为 35，最大连线 ID 为 960。

### 模型

- 在 `models/ultralytics/bbox/` 中加入脸部、眼睛、手部、足部和乳首细化检测模型。
- 大于 GitHub 单文件限制的模型使用 Git LFS 管理。

## R-1.0

- 首个发布版本，包含 SDXL / Anima 双底图、ControlNet、图生图、放大、部位细化、提示词工具、LoRA 管理和显存管理节点。
